# Cognitive Workspace + Object-Graph Rewrite v2 development gate

Status: **verified negative representation gate with one exact unique
development recovery**.

On the frozen, outcome-exposed 50-family ARC-TGI development cohort, the
query-blind two-stage rewrite creates one novel output opportunity.  That
candidate is exact and unique over both the frozen baseline and the equal-cost
cold arm, but the preregistered opportunity gate requires at least 5/50.
Prospective reserve materialization, oracle opening, and controller training
therefore remain unauthorized.

The failure is localized after parent construction: 49/50 tasks have an
execution-valid parent and 48 retain all four parents, but 48 tasks produce no
demo-exact second-stage composition.  The best parent residual has a median of
103 mismatched demo pixels; only four tasks are within eight pixels.  The
current whole-stage `stages[1]` hole is therefore too coarse and most parents
are not repairable near misses.

`summary.json` contains payload-free aggregate evidence.  The paired candidate
freezes and paired score files are byte-identical; `artifact_sha256.json`
records their sizes and SHA-256 values.  Raw task-level files remain protected
as manifest-only publication entries.

See `notes/results/cognitive-workspace-object-graph-rewrite-v2-20260811.md` for
failure attribution and the next bounded representation hypothesis.
