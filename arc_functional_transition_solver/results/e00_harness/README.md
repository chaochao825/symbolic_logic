# Legacy E00 evidence

This directory is retained for audit history. Its D4 counts and scores were
independently reproduced, but its manifests predate the current atomic bundle,
pinned scorer-blob identity, and archived-source format. Its source fingerprint
does not match the current implementation, so it must not be used as current
Gate-0 evidence. Use `../e00_harness_v2/` instead.
