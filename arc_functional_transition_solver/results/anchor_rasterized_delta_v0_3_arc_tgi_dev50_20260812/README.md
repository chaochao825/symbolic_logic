# Anchor-rasterized delta v0.3 dev50 evidence

Status: **valid outcome-exposed development sensor null**.

The finite `AnchorRasterizedDeltaNode` passes its controlled and dependency
checks, but strict leave-one-demo-out support prediction succeeds on only 1/50
tasks versus the preregistered 3/50 threshold.  No query output is novel over
the incumbent frontier and unique recovery is 0/50.  This directory does not
support an ARC-AGI generalization, useful-mask, controller, or brain-mechanism
claim.

Key files:

- `candidate_freeze_{a,b}.json`: independent query-blind candidate freezes;
- `result_{a,b}.json`: post-freeze outcome scoring;
- `failure_audit.json`: machine-readable parent/task failure matrix, baselines,
  frontier counts, and aggregate cost;
- `replay_sha256.txt`: byte-identity receipts for both freezes and scores;
- `source_sha256.txt` and `source_commit.txt`: exact implementation binding;
- `*.status`, `*.stdout.log`, and `*.stderr.log`: validation and execution
  receipts;
- `run.sh`: complete replay command.

See `notes/results/anchor-rasterized-delta-v0.3-20260812.md` for the claim
boundary and `notes/research/anchor-rasterized-delta-v0.3-gate-20260812.md` for
the preregistration.
