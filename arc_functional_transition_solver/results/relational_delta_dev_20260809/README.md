# Relational delta-program v0.2 development artifacts

This directory contains the outcome-exposed ARC-AGI-2 public-training
development gate for `afts-relational-delta-dsl/v0.2`.

- `semantic_tests.xml`: named semantic fixture receipt for all four render
  operations, topology rejection, near-miss guards, serialization, and novelty;
- `candidate_freeze.json`: query-blind, replay-identical candidates and native
  action ledgers;
- `result.json`: post-freeze public-training query scoring;
- `artifact_manifest.json`: content hashes and immutable result identifiers;
- `*.stderr.log`: retained empty error streams;
- `semantic_tests.stdout.log`: concise pytest receipt.

The cohort and diagnostic queries were already outcome-exposed.  These files
support implementation and representation-reachability conclusions only.  No
ARC-AGI-2 evaluation data was read, no model was trained, and neither controller
training nor visual LODO routing is authorized.

The bounded outcome is 2/50 unique pass@2 from the new provider and 0 natural
typed-repair recoveries because no legal content-novel frontier exists.
