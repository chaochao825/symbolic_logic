# Object–Program Workspace v1 controlled lane

Status: **verified controlled actuator pass; sensor attribution not supported**.

On 12 deterministic selector-fault tasks, all 12 contain a novel executable
output frontier.  The visual, residual-cleared, and posterior-shuffled typed
arms each recover 12/12; equal-cost cold restart recovers 5/12.  Visual evidence
has zero recovery unique over all controls, so this lane validates typing,
intervention, replay, and cost accounting only.  It does not license a visual
generalization or functional-switching claim.

`summary.json` contains payload-free aggregate evidence.  `artifact_sha256.json`
records every raw artifact by name, bytes, and SHA-256.  Raw tasks, solutions,
posterior samples, and candidate freezes are protected as manifest-only GitHub
publication entries.

See `notes/results/object-program-workspace-v1-20260811.md` for the complete
claim boundary.
